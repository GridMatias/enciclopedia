<!--
TEMPLATE — copy into <project>/CHANGELOG.md, never edit in place.
Append-only, newest first. One line per change, always with a reason and paths.
The agent reads only the last ~30 lines, so keep entries dense and dated.
Delete this comment when you copy the file. The parser tolerates it, the linter
warns (TEMPLATE-BANNER), and `python _tools/enc_new.py --print <slug>` strips it.
-->
---
id: <project-slug>/CHANGELOG
title: Changelog — <Nome progetto>
project: <project-slug>
type: log
status: active
lang: it
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
---

# Changelog — <Nome progetto>

Formato: `- YYYY-MM-DD — <cosa è cambiato> (<perché>) → <file toccati>`
Tag opzionali all'inizio del testo: `[decisione]` `[contenuto]` `[struttura]` `[import]` `[export]` `[fix]`.

## 2026

- <YYYY-MM-DD> — [struttura] progetto creato (<motivo>) → `README.md`, `CHANGELOG.md`
